{
  "name": "erc-9999",
  "version": "0.1.0",
  "private": false,
  "license": "CC0-1.0",
  "description": "ERC-9999 Asynchronous Register Projection for NFTs — interfaces, reference implementation and invariant tests",
  "scripts": {
    "lint": "node scripts/lint.js",
    "compile": "hardhat compile && node scripts/flatten-artifacts.js",
    "verify:constants": "node scripts/check-frozen-erc-constants.js",
    "verify:secret-scan": "node scripts/secret-scan.js",
    "verify:imports": "node scripts/check-imports.js",
    "test": "hardhat test --no-compile",
    "build": "node scripts/build.js",
    "verify:all": "npm run lint && npm run compile && npm run verify:constants && npm run verify:secret-scan && npm run verify:imports && npm test && npm run build"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "3.1.3",
    "ethers": "6.17.0",
    "hardhat": "2.29.0"
  }
}
